pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script{
                    sh '''
                    echo "TOKEN_DOCKER=$TOKEN_DOCKER" >> .env
                    bash docker_build.bash "$TOKEN_DOCKER" "$TOKEN_GIT"
                    '''
                }
            }
        }
    }
    post {
        always {
            deleteDir()
        }
    }
}