pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script{
                    sh ''' 
                    bash docker_build.bash "$TOKEN_DOCKER" "$TOKEN_GIT"
                    '''
                }
            }
        }
    }
    post {
        always {
            deleteDir()
        }
    }
}